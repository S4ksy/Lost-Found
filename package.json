{
  "name": "lost-and-found-system",
  "version": "1.0.0",
  "description": "Campus Lost and Found Management System",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -s . -l $PORT",
    "build": "echo 'Building static site' && cp -r . /opt/render/project/src/ || echo 'Build complete'"
  },
  "keywords": ["lost", "found", "campus", "management"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}